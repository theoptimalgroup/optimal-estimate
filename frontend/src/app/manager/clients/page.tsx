import { RolePagePlaceholder } from "@/components/layout/role-page-placeholder";

export default function ManagerClientsPage() {
  return (
    <RolePagePlaceholder
      title="Clients"
      description="Browse client accounts and quote history from a manager perspective."
    />
  );
}
